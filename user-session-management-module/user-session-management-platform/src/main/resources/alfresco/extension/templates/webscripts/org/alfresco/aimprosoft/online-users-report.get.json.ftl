<#escape x as jsonUtils.encodeJSONString(x)>
{
"devices":
        {
            "mobile": "${MOBILE!0}",
            "desktop": "${NORMAL!0}",
            "tablet": "${TABLET!0}"
        }
}
</#escape>